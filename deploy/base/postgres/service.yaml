apiVersion: v1
kind: Service
metadata:
  name: embassywatch-postgres
  labels:
    app: embassywatch
    component: postgres
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: embassywatch
    component: postgres
