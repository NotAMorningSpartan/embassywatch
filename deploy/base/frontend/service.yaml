apiVersion: v1
kind: Service
metadata:
  name: embassywatch-frontend
  labels:
    app: embassywatch-frontend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: embassywatch-frontend
